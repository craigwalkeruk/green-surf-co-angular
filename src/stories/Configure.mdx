import { Meta } from "@storybook/addon-docs/blocks";

import Styling from "./assets/styling.png";
import Assets from "./assets/assets.png";
import Testing from "./assets/testing.png";

<Meta title="Green Surf Co / Developer Guide" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Green Surf Co Component Library

    Welcome to the Storybook environment for **Green Surf Co**. This space serves as our source of truth for UI components, built with **Angular v21** and **Tailwind CSS v4**.
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <img src={Styling} alt="Logos of styling technologies" />
      <h4 className="sb-section-item-heading">Styling with Tailwind CSS</h4>
      <p className="sb-section-item-paragraph">
        We use **Tailwind CSS v4** for all styling. Utilities are available globally.
        Refer to `src/styles.css` for theme configuration and custom layer definitions.
      </p>
    </div>

    <div className="sb-section-item">
      <img src={Testing} alt="Screenshot of tests" />
      <h4 className="sb-section-item-heading">Testing & Validation</h4>
      <p className="sb-section-item-paragraph">
        Our stack includes **Vitest** and **Angular Testing Library**.
        Use the `Play` function in stories to write interaction tests that run in the browser.
        Run `npm run test` for the full unit test suite.
      </p>
    </div>

    <div className="sb-section-item">
      <img src={Assets} alt="Representation of typography and images" />
      <h4 className="sb-section-item-heading">Assets & Public Files</h4>
      <p className="sb-section-item-paragraph">
        Static assets like images and fonts are located in the `public/` directory.
        These are served at the root path during development and build.
      </p>
    </div>
  </div>

  <div className='sb-section-title'>
    ### Guidelines Recap
    - **Signals**: Use Signals for state management and `computed()` for derived state.
    - **Standalone**: All components must be standalone (Angular v21 default).
    - **Accessibility**: All components MUST pass AXE accessibility checks.
    - **Performance**: Use `OnPush` change detection and `NgOptimizedImage` where applicable.
  </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
    font-family: 'Inter', sans-serif;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
    margin-bottom: 40px;
  }

  img {
    object-fit: cover;
    border-radius: 8px;
    margin-bottom: 12px;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section-item {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .sb-section-item-heading {
    padding-top: 12px !important;
    padding-bottom: 8px !important;
    margin: 0 !important;
    font-weight: 700;
  }

  .sb-section-item-paragraph {
    margin: 0;
    font-size: 14px;
    line-height: 1.6;
    color: #4B5563;
  }

  @media screen and (max-width: 600px) {
    .sb-section {
      flex-direction: column;
    }
  }
  `}
</style>
